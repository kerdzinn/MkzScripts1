--// MKZ Loading Screen (Executor Version)
--// Feito por ChatGPT, versão ajustada (corrige bug da borda grossa no início)

pcall(function()
    -- Remove GUIs padrão do Roblox
    local StarterGui = game:GetService("StarterGui")
    StarterGui:SetCore("TopbarEnabled", false)
    for _,v in pairs(game:GetService("CoreGui"):GetChildren()) do
        if v:IsA("ScreenGui") then
            v.Enabled = false
        end
    end
end)

-- Cria GUI principal
local gui = Instance.new("ScreenGui")
gui.Name = "MkzLoadScreen"
gui.IgnoreGuiInset = true
gui.ResetOnSpawn = false
gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
gui.Parent = game:GetService("CoreGui")

-- Fundo escuro
local bg = Instance.new("Frame")
bg.Size = UDim2.new(1, 0, 1, 0)
bg.BackgroundColor3 = Color3.fromRGB(10, 10, 12)
bg.ZIndex = 9999
bg.Parent = gui

-- Gradiente de fundo
local bgGradient = Instance.new("UIGradient", bg)
bgGradient.Color = ColorSequence.new{
	ColorSequenceKeypoint.new(0, Color3.fromRGB(0, 0, 0)),
	ColorSequenceKeypoint.new(1, Color3.fromRGB(10, 10, 30))
}
bgGradient.Rotation = 90

-- Título
local title = Instance.new("TextLabel")
title.Text = "Mkz"
title.Font = Enum.Font.GothamBold
title.TextColor3 = Color3.fromRGB(0, 140, 255)
title.TextScaled = true
title.Size = UDim2.new(0.3, 0, 0.08, 0)
title.Position = UDim2.new(0.5, 0, 0.43, 0)
title.AnchorPoint = Vector2.new(0.5, 0.5)
title.BackgroundTransparency = 1
title.ZIndex = 10001
title.Parent = gui

-- Moldura da barra
local barFrame = Instance.new("Frame")
barFrame.Size = UDim2.new(0.42, 0, 0.045, 0)
barFrame.Position = UDim2.new(0.5, 0, 0.55, 0)
barFrame.AnchorPoint = Vector2.new(0.5, 0.5)
barFrame.BackgroundColor3 = Color3.fromRGB(70, 70, 78)
barFrame.ZIndex = 10000
barFrame.Parent = gui

local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0, 12)
corner.Parent = barFrame

-- Barra de progresso (correção aqui 👇)
local fill = Instance.new("Frame")
fill.Size = UDim2.new(0, 0, 1, 0)
fill.BackgroundColor3 = Color3.fromRGB(0, 120, 255)
fill.ZIndex = 9999
fill.ClipsDescendants = true -- <- impede o arredondamento exagerado no início
fill.Parent = barFrame

local fillCorner = Instance.new("UICorner")
fillCorner.CornerRadius = UDim.new(0, 12)
fillCorner.Parent = fill

local fillGrad = Instance.new("UIGradient", fill)
fillGrad.Color = ColorSequence.new{
	ColorSequenceKeypoint.new(0, Color3.fromRGB(0, 180, 255)),
	ColorSequenceKeypoint.new(1, Color3.fromRGB(0, 100, 255))
}

-- Sombra
local shadow = Instance.new("ImageLabel")
shadow.AnchorPoint = Vector2.new(0.5, 0.5)
shadow.Position = barFrame.Position + UDim2.new(0, 0, 0.06, 0)
shadow.Size = barFrame.Size + UDim2.new(0, 20, 0, 20)
shadow.Image = "rbxassetid://1316045217"
shadow.ImageTransparency = 0.6
shadow.BackgroundTransparency = 1
shadow.ZIndex = 9998
shadow.Parent = gui

-- Texto de porcentagem
local percentLabel = Instance.new("TextLabel")
percentLabel.Text = "0%"
percentLabel.Font = Enum.Font.Gotham
percentLabel.TextColor3 = Color3.fromRGB(200, 200, 210)
percentLabel.TextScaled = true
percentLabel.Size = UDim2.new(0.1, 0, 0.05, 0)
percentLabel.Position = UDim2.new(0.5, 0, 0.61, 0)
percentLabel.AnchorPoint = Vector2.new(0.5, 0.5)
percentLabel.BackgroundTransparency = 1
percentLabel.ZIndex = 10001
percentLabel.Parent = gui

-- Carregamento suave
local TweenService = game:GetService("TweenService")
for i = 1, 100 do
	local progress = i / 100
	TweenService:Create(fill, TweenInfo.new(0.05, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
		Size = UDim2.new(progress, 0, 1, 0)
	}):Play()
	percentLabel.Text = i .. "%"
	wait(0.05)
end

wait(0.6)
gui:Destroy()
